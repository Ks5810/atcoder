#include <bits/stdc++.h>
using namespace std;

int main()
{
    int tmp, i = 1;

    while(cin >> tmp)
    {
        if(tmp == 0)
        {
            cout << i << "\n";
        }
        i++;
    }
    return 0;
}